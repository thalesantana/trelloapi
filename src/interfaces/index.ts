export type List = {
    id: number
    name: string
    close: boolean
    pos: number
    softLimit: string,
    idBoard: string
    subscribed: string
}

export type LabelList = {
    id: string,
    idBoard: string,
    name: string,
    color: string
}